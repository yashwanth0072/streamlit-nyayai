# Web Framework
streamlit>=1.28.0

# HTTP Requests (for OpenRouter API: all AI providers)
requests>=2.31.0

# PDF Processing
PyMuPDF>=1.23.0

# Note: All AI providers (Gemini, OpenAI, DeepSeek, Nemotron) now use OpenRouter API
# You only need ONE OpenRouter API key from https://openrouter.ai/keys

# Note: Python standard library modules used (no installation needed):
# - sqlite3 (database)
# - json (data serialization)
# - os (environment variables)
# - io (file operations)
# - typing (type hints)
# - abc (abstract base classes)